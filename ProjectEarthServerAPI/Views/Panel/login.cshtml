<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: url('/images/backgrounds/login.avif') no-repeat center center fixed;
            background-size: cover;
            font-family: 'Roboto', sans-serif;
            backdrop-filter: blur(6px);
            transition: backdrop-filter 0.5s ease; /* Smooth transition for the blur effect */
        }

        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .card {
            background-color: rgba(255, 255, 255, 0.8);
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
            text-align: center;
            transition: all 0.3s ease; /* Smooth transition for the card */
        }

        .card:hover {
            transform: translateY(-5px); /* Lift the card slightly on hover */
            box-shadow: 0 0 30px rgba(0, 0, 0, 0.2); /* Add a slightly larger shadow on hover */
        }

        .card-title {
            font-weight: 700;
            margin-bottom: 30px;
            font-size: 2.5rem;
            letter-spacing: 2px;
            color: #333;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
        }

        .form-group {
            margin-bottom: 20px;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            width: 100%;
            font-size: 1.2rem;
            padding: 12px;
            border-radius: 20px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }

        /* Personalización del input */
        .form-control {
            border-radius: 10px; /* Rounded corners */
            border-color: #ced4da; /* Color del borde */
            font-size: 1.1rem;
            padding: 15px;
            background-color: rgba(255, 255, 255, 0.9);
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px); /* Blur */
        }

            /* Personalización del hover del input */
            .form-control:focus {
                border-color: #007bff; /* Cambia el color del borde al enfocar */
                box-shadow: none; /* Elimina la sombra del focus */
            }

        /* Estilo para el contenedor de la imagen */
        .logo-container {
            margin-bottom: 20px;
        }

        /* Estilo para la imagen */
        .logo {
            width: 100px;
            height: auto;
            filter: drop-shadow(2px 2px 4px rgba(0,0,0,0.3));
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="logo-container">
                <img src="/images/logo/icon.png" alt="Logo" class="logo">
            </div>
            <h2 class="card-title">LOGIN</h2>
            <form asp-action="Login" method="post">
                <div class="form-group">
                    <input type="password" class="form-control" id="password" name="password" placeholder="Password" required>
                </div>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>
    </div>
</body>
</html>
